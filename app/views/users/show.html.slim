ul.memberships
  - @user.memberships.each do |membership|
    li
      = link_to membership.group.name, group_user_path(membership.group, membership.user)
      small
        | &nbsp;&mdash;&nbsp;
        = t membership.role
